# Credentials & secrets
auth.json
models.json

# Session transcripts (may contain sensitive conversation content)
sessions/

# Work-specific local extensions, not for the public config repo
extensions/orca-*

# Auto-generated by pi (npm/ has its own .gitignore ignoring everything inside)
node_modules/

# Runtime caches & local state (machine-generated, not portable)
mcp-cache.json
mcp-npx-cache.json
mcp-onboarding.json
models-store.json
pi-cache-optimizer-stats.json
pi-cache-optimizer-stats.json.*.tmp
run-history.jsonl
state/
trust.json

# Local agent symlinks (point to machine-local ai-assets, not portable)
agents/

# OS
.DS_Store
